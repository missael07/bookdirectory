import { BookOverviewPipe } from './book-overview.pipe';

describe('BookOverviewPipe', () => {
  it('create an instance', () => {
    const pipe = new BookOverviewPipe();
    expect(pipe).toBeTruthy();
  });
});
